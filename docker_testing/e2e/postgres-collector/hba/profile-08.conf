# profile-08: cert auth attempt (commented + fallback md5)
# hostssl all           all             0.0.0.0/0               cert clientcert=1
host    all             all             0.0.0.0/0               md5
host    all             all             10.8.0.0/16             reject
